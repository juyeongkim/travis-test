# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
sudo: false
cache: packages

jobs:
  include:
    - r: bioc-devel
      script: echo devel
    - r: bioc-release
      script: echo release
    - stage: code coverage
      script:
        - echo code coverage

